# 2026-06-12 — P1 kickoff: classification, workspace repo, extension layer extracted

**Session:** pg-ai-stewards lane (7ea7faa4), same-day continuation after the
anatomy/PR.1/pgEdge arc. Michael: "Lets kick off P1!"

## What shipped

1. **`github.com/cpuchip/pg-ai-stewards-workspace` (private) created** at
   `projects/pg-ai-stewards-workspace/` — the ratified downstream overlay
   repo. Skeleton per spec §Overlay design; covenant.yaml + intent.yaml
   overlay copies with provenance headers (source of truth stays in the
   monorepo until cutover); `.env.example` is the full env-name inventory
   (names only).
2. **Classification complete** — `overlays/classification.tsv`, all 241
   live SQL files (238 manifest + 2 init + 1 scratch), verified 1:1
   against the manifest: **191 core / 17 core-p2 / 27+1 overlay / 5 mixed
   / 1 scratch.** Two reclassifications during execution: 3e2-3 (grant
   seeds found at its tail → mixed) and pe5 (domain-flavored through →
   whole-file overlay).
3. **Workspace overlays populated** — 33-entry
   `overlays/migration-manifest.txt` in original relative order: init-01
   workstream seeds + 5 split seed-halves + 27 whole-file overlay
   migrations (original filenames kept for parity diffs).
4. **OSS extension layer extracted** (`3d8229d`, pushed after the gate):
   `src/*.rs` (audited — no secrets, no personal paths), lib.rs
   `extension_sql_file!` chain reworked (3e2-4/5/6/7 embeds removed,
   requires repaired at 4a), Dockerfile COPY list repaired, 189 core
   migrations + 5 split files, 193-entry `migration-order.txt`,
   extension/README.md documenting the two-tier SQL design.
5. **Verification (inverse-hypothesis):** docker build green
   (`stewards-oss-pg:p1-verify`) AND a scratch-container virgin
   `CREATE EXTENSION` — `stewards.version()` = 0.2.0, **0 mcp_servers
   rows, 0 gospel grants, only generic agents** (stewards-explore,
   watchman-consolidator). The splits provably kept workspace seeds out
   of the public bundle. Scratch container removed.

## Findings (these bind later P1 work)

- **The live runner is lexical and manifest-blind** (migrate.go:
  `sort.Strings` over `extension/*.sql`). Proven consequence: the bridge
  replayed `scratch-ct2-run2-codewright.sql` into the live DB on
  2026-06-10 (durable codewright-ct2 agent + pipeline rows; ledger row
  `scratch-ct2-run2-codewright`). **Disposition of those rows is
  Michael's call** — folds into the migrate-manifest design call. The
  OSS runner must consume explicit manifests (core tier + overlay tier),
  never a directory glob.
- **Private manifest drift repaired** (root `e5ccc0c3`): nine migrations
  applied live 2026-06-09..10 (r11–r17, ct2-5, ct2-7e) were never
  appended to migration-order.txt; restored in ledger applied_at order.
  Plausibly intersects the 20 unclassified live↔repo mismatches —
  confirm at the next verify-suite run.
- **The checked-in bundle was stale** — it still carried pre-split
  WS1–WS9 workstream seeds (incl. personal text) that the live runner
  re-applied on every fresh boot. OSS decision: the bundle is a build
  artifact, generated by `cargo pgrx package`, never checked in, never a
  manifest entry.
- **Secret scan of the full chain: clean.** Personal/Windows paths only
  in overlay/mixed seed sections (now split out) and the stale bundle
  (not extracted).

## Carry-forward (next P1 sessions)

1. **Go daemons** — re-home modules `github.com/cpuchip/scripture-study/...`
   → `github.com/cpuchip/pg-ai-stewards/...`: stewards-mcp (bridge),
   stewards-cli, persona-host, fs-read-mcp. Kill the go.work stub knot.
2. **Runner-consumes-manifest** — two-tier (core + overlay dir mount),
   ledger naming normalized (suffix-less convention; the manual-`.sql`
   wart), exclusion of non-manifest files by design.
3. **Compose** — one docker-compose.yml, `stewards-oss-*` namespace,
   ports 55434/8081/8091, no fixed-name volumes, overlay mount point.
4. **Seed pack** — generic covenant/intent templates (post-PR.1
   machinery: presiding included); consider one-lineage with the
   ai-jumpstart kit (crossover proposal defers the call to this council).
5. **Runtime-tier replay proof** — scratch container + manifest replay
   (needs the extracted CLI); then the verify-suite as CI.
6. Genericization worklist (tracked in classification.tsv notes): bundle
   researcher text, yaml.rs hardcoded slug, k4/j5/j8c/j9c/j12 intent
   bindings, scripture_anchor design call, redline% trigger predicates.

## Relational

Michael's kickoff was three words; everything else ran on the ratified
spec + the classification TSV as the load-bearing instrument. The
covenant's exercise_stewardship clause did real work twice: the manifest
repair (obviously-yes fix, done and reported) and the scratch-file
disposition (durable live rows — surfaced, NOT acted on).
